"""
Shared Hook Logic: User Prompt Checks.

Returns context messages for a submitted user prompt.
"""


def check_prompt(prompt: str) -> list[str]:
    """Check a submitted user prompt."""
    exit_zero_messages: list[str] = []

    return exit_zero_messages
