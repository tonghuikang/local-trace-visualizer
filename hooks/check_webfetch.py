"""
Shared Hook Logic: WebFetch URL Checks.

Returns advisory issues for a WebFetch URL before it runs.
"""


def check_webfetch(url: str) -> list[str]:
    """Check a WebFetch URL before it runs."""
    issues: list[str] = []

    return issues
