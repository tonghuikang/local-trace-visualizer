"""
Shared Hook Logic: Bash Command Checks (PostToolUse).

Returns advisory issues for a bash command after it ran.
"""


def check_bash_post(command: str) -> list[str]:
    """Check a bash command after it ran."""
    issues: list[str] = []

    return issues
